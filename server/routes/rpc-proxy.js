import express from "express";

const router = express.Router();

router.post('/rpc', async (req, res) => {
    //TODO: make proxy | mocking data from some session, handler, some stub
})

export default router;
